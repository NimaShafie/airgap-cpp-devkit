# ── clang-llvm-style-formatter ──────────────────────────────────────────────
# Add these entries to the .gitignore of any repository that uses
# clang-llvm-style-formatter (either directly or as a submodule).

# Python venv created by bootstrap.sh — generated locally, never committed.
# Adjust the path if the submodule lives under a different name.
clang-llvm-style-formatter/.venv/

# Per-developer local config overrides — intentionally not shared.
# Created by install-hooks.sh inside the submodule directory.
clang-llvm-style-formatter/.llvm-hooks-local/

# clang-llvm-source-build binaries — generated by the optional source build.
# Remove these two lines if you are not using the source-build path.
clang-llvm-source-build/bin/windows/clang-format.exe
clang-llvm-source-build/bin/linux/clang-format

# .clang-format and .clang-tidy live inside the submodule (config/ directory)
# and are not copied to the host repo root. No entries needed here for them
# unless you create local overrides at the repo root — in which case add:
# .clang-format
# .clang-tidy
# ─────────────────────────────────────────────────────────────────────────────