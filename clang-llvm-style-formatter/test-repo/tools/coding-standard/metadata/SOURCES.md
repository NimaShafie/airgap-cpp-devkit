# Tool Sources

This standards kit can operate with:

- LLVM tools already installed on the user's PATH
- LLVM tools supplied by an internal network path
- Portable LLVM binaries bundled under `vendor/`

This package does not require administrator privileges.
